{
  "name": "@devnode/composedb",
  "version": "1.0.0",
  "author": "",
  "license": "MIT",
  "private": true,
  "main": "./dist/index",
  "typings": "./dist/index",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  "scripts": {
    "build": "tsup",
    "clean": "rimraf .turbo node_modules dist",
    "composedb:daemon": "dotenv cross-var CERAMIC_ENABLE_EXPERIMENTAL_COMPOSE_DB='true' ceramic daemon --hostname 0.0.0.0 --port=%PORT% --config ./daemon.config.json",
    "composedb:server": "composedb graphql:server --ceramic-url=%CERAMIC_NODE% --graphiql ./gen/runtime-composite.json --port 7008",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "resolutions": {
    "graphql": "^16.5.0"
  },
  "devDependencies": {
    "@devnode/tsconfig": "*",
    "tsup": "^6.2.3",
    "rimraf": "^3.0.2",
    "@ceramicnetwork/cli": "next",
    "@composedb/cli": "next"
  },
  "packageManager": "yarn@1.22.19"
}
